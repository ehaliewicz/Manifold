#ifndef BUNCH_RENDER_H
#define BUNCH_RENDER_H

#include <genesis.h>

void prepare_bunches(u16 src_sector, u32 cur_frame);
//void draw_bunches();
void draw_bunches(u16 src_sector, u32 cur_frame);


#endif